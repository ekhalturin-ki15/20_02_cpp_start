#include <iostream>
#include <cmath>
#include <string>
using namespace std;

int main()
{
    //int a, b, n;

    //cin >> a >> b >> n;
    ////a = 10, b = 15, n = 2;

    //cout << int(a * n + b * n / 100) << " " << int(b * n) % 100;
    //"%a%"
#ifdef _DEBUG
    FILE* IN, * OUT;
    freopen_s(&IN, "input.txt", "r", stdin);
    freopen_s(&OUT, "output.txt", "w", stdout);
#endif



    string s;

    while (getline(cin, s))
    {
        cout << '\n';
        cout << '\0';

    }



    //int max1;// = -100000000;
    //int max2;
    //int a;
    //cin >> a;
    //max2 = max1 = a;

    //while (true)
    //{
    //    cin >> a;
    //    if (a == 0) 
    //        break;
    //    
    //    if (a >= max1)
    //    {
    //        max2 = max1;
    //        max1 = a;            
    //    }

    //    if (max1 > a && a >= max2)
    //    {
    //        max2 = a;
    //    }

    //}

    //abs(2- 4 + 1);

    //cout << max2;



    /*int a, b, c, d;
    cin >> a >> b >> c >> d;
    for (int i = a; i <= b; ++i)
    {
        if (i % d == c) cout << i << " ";
    }*/




    //int sum = 0, a;

    //while (true)
    //{
    //    cin >> a;
    //    if (a == 0) break;
    //    sum += a;

    //    //if (sum > 1000) break;
    //}


    /*
    do
    {
        cin >> a;
        sum += a;
    } while (a != 0)
    */



    /*a = 1;
    while (a != 0)
    {
        cin >> a;
        sum += a;
    }
    cout << sum;*/



}